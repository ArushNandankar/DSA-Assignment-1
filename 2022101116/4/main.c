#include "functions.h"

int main () {
    PtrToRowNode Start = CreateRowList ();
    char OPER [4];
    scanf("%s",OPER);

    if (OPER[0] == 'T') {
        int N,M,K;
        for (int i = 0; i < K ; i++)
        {
            int i,j,val;
            scanf("%d %d %d",&i,&j,&val);
        }
        
    }

    else if (OPER[0] == 'A') {
        
    }

    else if (OPER[0] == 'M') {
        
    }
}
